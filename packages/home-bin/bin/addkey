#!/usr/bin/env bash

if [[ -z $1 ]]; then
  echo "usage: addkey 'target-host-name'"
  exit 1
fi

host=$(hostname -s)

ssh-keygen -t ed25519 -C "forrest@$host" -f "$HOME"/.ssh/"$1"
mkdir -p ~/projects/dots/keys/"$1"
cp "$HOME"/.ssh/"$1".pub ~/projects/dots/keys/"$1"/"$host".pub
